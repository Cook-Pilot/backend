# #16 사용자 최근 조리 레시피

별도 서버 조리 세션을 만들지 않고, 조리 완료 후 저장되는
`post_cook_reviews`를 완료 이력으로 사용한다.

`GET /api/v1/home/recent-recipes`는 현재 사용자의 리뷰를 최신순으로 읽고,
레시피별 최신 한 건만 남겨 최대 10개를 반환한다. 레시피와 개인 레시피 존재
여부를 묶어서 조회해 목록 항목마다 DB를 다시 조회하는 N+1을 피한다.

신규 사용자처럼 조리 이력이 없으면 `[]`를 반환한다.
